customer@gmail.com
Nedva@54

directory in the settings
run migrate command
create superuser
configure template inheritance
collect static

if pdadmin refused to start follow this process
I found the same issue when upgrading to pgAdmin 4 (v1.6). On Windows I found that clearing
 out the content 
inside C:\Users\%USERNAME%\AppData\Roaming\pgAdmin\sessions folder fixed the issue for me

after configuring postgres install psycopg2

postgres configuration

DATABASES = {
    'default': {
        'ENGINE': "django.db.backends.postgresql",
        'NAME': 'booknride',
        'USER': 'postgres',
        'PASSWORD': 'Letmein123',
        'HOST': 'localhost',
    }
}

db configuration for my sql and you have to install mysqlclient before running migration
DATABASES = {
    'default': {
        'ENGINE': "django.db.backends.mysql",
        'NAME': 'hotelDB',
        'USER': 'root',
        'PASSWORD': 'Nedva@54',
        'HOST': 'localhost',
    }
}

install  pip install python-decouple
after setting your env file add this DEBUG = config('DEBUG', cast = bool)
delete your database and recreate again after creating the custom user model
also delete migration files



I stopped at redireting users to their respective dashboard
next i need to design the dashboard and also restrict users from accesing other dashboards





mb: Margin Bottom
mt: Margin Top
ml: Margin Left
mr: Margin Right
mx: Margin Left and Right (Horizontal)
my: Margin Top and Bottom (Vertical)